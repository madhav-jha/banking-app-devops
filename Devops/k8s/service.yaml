apiVersion: v1
kind: Service
metadata:
  name: banking-app-service
spec:
  selector:
    app: banking-app
  ports:
    - port: 82          
      targetPort: 82    
      nodePort: 31002   
  type: NodePort
